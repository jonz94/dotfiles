function ginit { git init && git add -A && git commit -m "initial commit" }
function gpoat { git push origin --all && git push origin --tags }
function sud { scoop update && scoop status }
function ud { scoop update && scoop status }
